# Deprecated — use lightwell-workshop

Cluster GitOps for the SDLC demo (OpenCode overlay, Argo apps, verify scripts, provisioning runbooks) lives in **`lightwell-workshop/automation/gitops/`**.

This directory remains in the repo only for historical Argo `repoURL` references. **Do not add new automation here.**

**SCM content for EDA** (rulebooks, playbooks, container source): repo root — `rulebooks/`, `eda-rulebooks/`, `playbooks/`, `container/`.

Tenant platform automation: `lightwell-workshop/automation/gitops/bootstrap-tenant` and `bootstrap-infra`.
