# GitOps: Event-Driven Ansible

Placeholder for EDA activation / rulebook mount (ConfigMap or git-sync sidecar pointing at `eda-rulebooks/` and `playbooks/`).

Application: `gitops/argocd/applications/sdlc-eda.yaml`
