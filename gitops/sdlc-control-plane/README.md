# GitOps: OpenCode control plane

Kustomize: namespace, [`../base/cluster-config`](../base/cluster-config), [`deployment.yaml`](deployment.yaml).

Application: `gitops/argocd/applications/sdlc-control-plane.yaml`

Prerequisites: [`../secrets/README.md`](../secrets/README.md), filled `cluster-config.yaml` (`OPENCODE_IMAGE`, URLs).
