---
# Collections required in the EDA decision environment image (or custom DE build).
# Stock image: quay.io/ansible/ansible-rulebook:main — verify ansible.eda is present.
collections:
  - name: ansible.eda
    version: ">=2.0.0"
